import { TimelineSeparator as TimelineSeparatorComponent } from '@mui/lab';

const TimelineSeparator = (props) => {
  return <TimelineSeparatorComponent {...props} />;
};

export default TimelineSeparator;
